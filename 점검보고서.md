# 삼성여고 2026 입시 — 전체 점검보고서

점검일: 2026-09-14  
점검 범위: 현재 `master` 코드, 최근 Git 이력, Supabase 운영 데이터베이스의 실제 테이블·권한 규칙·성능 진단 결과  
코드 변경: 없음. 이 파일만 요청에 따라 새로 작성함.

## 먼저 보는 결론

현재 앱은 학생·담임교사·전체관리자 역할에 맞는 핵심 화면이 대부분 실제로 연결되어 있습니다. 원서 관리, 상담 예약, 입결·모집정보 조회, 경쟁률 조회, 합격 가능성 계산, 일정 관리, 관리자용 데이터 업로드까지 한 흐름으로 구현되어 있습니다. 코드에서 `TODO`, `FIXME`, 더미 데이터, 화면에 연결되지 않은 주요 기능은 확인하지 못했습니다.

다만 앞으로 정리할 때 우선 봐야 할 부분은 네 가지입니다.

1. **입결·모집정보·작년 경쟁률이 서로 다른 원본 데이터를 써서, 같은 전형이라도 이름이 달라 자동 연결이 다시 실패할 수 있습니다.** 이미 별칭·유사도 비교·수동 선택으로 보완했지만, 근본적으로 한 곳에서 관리하는 대응표가 없습니다.
2. **학년 공통 일정은 로그인하지 않은 사람도 읽을 수 있게 되어 있습니다.** 원래 의도가 “로그인한 학생·교사 전체”였다면 권한 규칙을 좁혀야 합니다.
3. **여러 목록 불러오기 기능이 실패해도 오류 메시지 없이 빈 목록처럼 보일 수 있습니다.** 인터넷이나 권한 문제가 생겼을 때 원인을 찾기 어렵습니다.
4. **합격 가능성 계산용 서버가 처음 실행될 때 약 5만 8천 건의 입결을 다시 읽어 계산합니다.** 지금도 동작하도록 보완되어 있지만, 접속자가 늘거나 서버가 새로 시작될 때 느려질 위험이 있습니다.

---

## 0. 점검 방법과 한계

### 확인한 것

- 앱 화면을 구성하는 `src/app`, `src/components`, `src/lib` 코드 전체의 파일 구성과 연결 관계
- 최근 30개 Git 변경 이력과 데이터베이스 변경 파일 41개(0001~0041)
- 운영 Supabase의 실제 공개 테이블 13개, 행 수, 권한 규칙(RLS), 저장소(원서 첨부파일) 권한, 데이터베이스 성능·보안 진단
- TypeScript 정적 검사와 ESLint 검사

### 확인 결과

- TypeScript 검사: 오류 없음
- ESLint: 오류 0개, 경고 1개
  - `AdmissionProbabilityCalculator.tsx`에서 화면 효과가 참조하는 `loadCards`를 의존성 목록에 넣지 않았다는 경고입니다. 당장 오류는 아니지만, 나중에 계산기 대상 학생이 바뀌는 흐름을 수정할 때 이전 학생 카드가 남는 식의 문제를 만들 수 있어 정리 대상입니다.
- 공개 테이블 13개 모두 RLS(행 단위 권한 제한)가 켜져 있습니다.
- 원서 첨부파일 저장소도 학생 본인·담당 담임·전체관리자 범위로 읽기·쓰기 제한이 걸려 있습니다.

### 한계

이 보고서는 코드와 운영 데이터베이스 설정을 기준으로 한 진단입니다. 모든 버튼을 모든 역할·모든 휴대폰에서 실제로 눌러 보는 전수 기능 시험은 하지 않았습니다. 따라서 아래의 “정상 동작”은 **코드상 화면·데이터·권한 연결이 확인된 상태**를 뜻합니다. 실제 기기별 화면 문제는 별도 시험이 필요합니다.

---

# 1. 현재 구현 상태

## 1-1. 실제 역할 구조

| 역할 | 코드상 구분 | 볼 수 있는 범위 |
|---|---|---|
| 학생 | `profiles.role = student` | 본인 원서·저장 항목·개인 일정, 자기 반 상담 슬롯, 반/학년 공개 일정 |
| 담임교사 | `role = teacher`, `teacher_role = homeroom` | 자기 담당 학급의 학생·원서·상담·학급 일정 |
| 전체관리자 | `role = teacher`, `teacher_role = admin` | 반 선택 후 모든 학급의 학생·원서·상담, 교사 계정, 입결/모집정보 업로드, 학년 공통 일정 |
| 겸용 관리자 | 담임교사 + `dual_admin`, 관리자 모드 | 관리자용 계정·전역 설정 권한은 가질 수 있으나, 코드상 다른 반 학생 정보는 보지 못하도록 자기 반 범위에 고정됨 |

## 1-2. 학생 화면

### 상담신청

| 기능 | 상태 | 코드에서 확인한 내용 |
|---|---|---|
| 날짜별 상담 슬롯 보기 | 정상 동작 | 학생 본인 반의 슬롯만 RLS로 조회하며, 실시간 변경 구독도 연결됨 |
| 시간 슬롯 예약·취소 | 정상 동작 | DB 함수가 같은 반 여부, 이미 예약됐는지, 하루 1개 제한을 서버에서 다시 검증함 |
| 이름만 있는 예비 슬롯 예약 | 정상 동작 | `예비1`처럼 시간 없이 이름만 있는 슬롯도 최근 데이터 구조에 반영됨 |
| 예약 현황 표시 | 정상 동작 | 본인 예약과 가능한 슬롯을 구분해 표시함 |

### 수시원서

| 기능 | 상태 | 코드에서 확인한 내용 |
| 원서 카드 추가·수정·삭제 | 정상 동작 | 학생 본인 카드에 대한 생성·수정·삭제 연결 확인 |
| 대학·학과·전형 단계 선택 / 자동 채움 | 정상 동작 | 모집정보 원본(`admission_offerings`) 기반 단계 선택과 전형 방법·모집 인원 등의 자동 채움 연결 확인 |
| 최근 입결 표시·수정 | 정상 동작 | 카드 안의 JSON 목록과 입결 원본 조회를 함께 사용 |
| 카드 순서 드래그 및 지망 순위 자동/수동 지정 | 정상 동작 | 일반 원서 순서와 접수 완료 원서 순서를 별도로 저장함 |
| 원서 첨부 이미지 | 정상 동작 | 업로드, DB 기록, 1시간짜리 서명 링크로 보기, 삭제 흐름 연결 확인 |
| 접수한 원서 표시 | 정상 동작 | 별표, 별도 보기, 수험번호, 여러 일정, 별도 순서가 구현됨 |
| 접수한 원서 일정의 캘린더 추가 | 정상 동작 | 날짜 형식이 확정된 일정만 골라 개별적으로 캘린더에 추가함 |

### 대입정보

| 기능 | 상태 | 코드에서 확인한 내용 |
| 입결 직접 검색 | 정상 동작 | 입결 원본에서 대학·학과·전형을 검색하고 연도별 결과를 표시함 |
| 작년 경쟁률 조회 | 정상 동작 | 별도 경쟁률 아카이브에서 시계열을 조회함 |
| 경쟁률 조회 저장·다시 열기·삭제 | 정상 동작 | `admission_competition_saves`에 학생별 저장, 다시 열기, 삭제가 연결됨 |
| 실시간 경쟁률 외부 링크 | 정상 동작 | 진학사·유웨이 링크가 별도 새 창으로 열리도록 구성됨 |
| 원서 카드에서 불러오기 | 정상 동작 | 카드의 대학·학과·전형을 조회 화면으로 넘기는 흐름이 있음 |

### 합격률계산기

| 기능 | 상태 | 코드에서 확인한 내용 |
| 유의사항 동의 후 사용 | 정상 동작 | 동의 시각을 프로필에 저장하고, 동의 전에는 계산기를 막음 |
| 과거 입결 기반 추정 | 정상 동작 | 로그인한 학생도 서버 계산 자료를 요청할 수 있고, 클라이언트에서 추정 계산을 수행함 |
| 원서 카드 불러오기 | 정상 동작 | 카드의 대학·학과·전형을 계산기 입력값으로 불러옴 |
| 예상 경쟁률 보조 조회 | 정상 동작 | 작년 경쟁률 팝업과 외부 실시간 경쟁률 링크가 있음 |
| 전형명 불일치 시 수동 재검색 | 정상 동작 | 계산기 입력값 자체를 바꾸지 않고, 팝업 안에서 경쟁률 원본을 다시 고를 수 있음 |
| 계산 결과 저장·불러오기·삭제 | 정상 동작 | 입력값과 결과 요약을 학생별로 저장함 |

### 입시일정

| 기능 | 상태 | 코드에서 확인한 내용 |
| 개인 일정 추가·수정·삭제 | 정상 동작 | 작성자 본인만 보도록 구현됨 |
| 반 일정·학년 공통 일정 보기 | 정상 동작 | 본인 반 및 학년 공통 일정을 읽도록 구성됨 |
| 내 접수 원서 일정 불러오기 | 정상 동작 | 접수 완료 표시된 카드의 일정만 후보로 보여 줌 |
| 교사의 원서 일정과 분리 | 정상 동작 | 학생과 교사가 각각 추가한 원서 일정은 작성자별로 분리되어 서로 보이지 않음 |

## 1-3. 담임교사 화면

### 상담관리

| 기능 | 상태 | 코드에서 확인한 내용 |
| 날짜별 슬롯 생성·삭제 | 정상 동작 | 시간 슬롯과 이름만 있는 슬롯을 모두 만들 수 있음 |
| 자주 쓰는 시간대 관리 | 정상 동작 | `slot_favorites`에 반별 즐겨찾기 저장 |
| 예약 학생 직접 배정·예약 취소·메모 | 정상 동작 | 슬롯 편집 창과 DB 수정 흐름이 연결됨 |
| 상담 현황 공개 설정 | 정상 동작 | `app_settings.status_reveal` 값을 읽고 바꾸는 기능이 연결됨 |
| CSV 내보내기 | 정상 동작 | 슬롯/예약 현황을 CSV로 만드는 코드가 있음 |

### 수시원서

| 기능 | 상태 | 코드에서 확인한 내용 |
| 담당 반 학생별 카드 보기·수정·삭제 | 정상 동작 | 담임 반 학생 카드만 RLS로 접근하도록 되어 있음 |
| 전체 카드/표 보기 | 정상 동작 | 카드형과 표형 보기가 구현됨 |
| 접수한 원서 보기 | 정상 동작 | 전체 보기와 별도 접수 완료 보기, 수험번호·일정 표시, 독립 순서가 있음 |
| 엑셀 일괄 내려받기 | 정상 동작 | 전체 원서와 접수 원서를 각각 다른 형식으로 내보냄 |
| 학생 원서 일정 캘린더에 추가 | 정상 동작 | 해당 교사가 자기 화면에서 별도로 추가할 수 있음. 학생이 추가한 일정과는 독립적임 |

### 대입정보 / 합격률계산기

| 기능 | 상태 | 코드에서 확인한 내용 |
| 특정 학생을 대상으로 입결·경쟁률 조회 | 정상 동작 | 학생 선택 후 학생별 저장 목록을 다루는 구조 |
| 특정 학생을 대상으로 합격 가능성 계산·저장 | 정상 동작 | 학생 카드 불러오기 및 학생별 저장 목록 지원 |

### 입시일정

| 기능 | 상태 | 코드에서 확인한 내용 |
| 교사 개인 일정 | 정상 동작 | 작성자 본인만 관리·열람 |
| 자기 반 공통 일정 작성·관리 | 정상 동작 | 담임 반 범위의 반 일정 생성·수정·삭제 |
| 학년 공통 일정 보기 | 정상 동작 | 읽기 가능. 작성 권한은 전체관리자에게만 있음 |

### 학생명단

| 기능 | 상태 | 코드에서 확인한 내용 |
| 학생 일괄 추가 | 정상 동작 | 학번·이름 줄 목록을 받아 현재 반에 추가 |
| 학생 제거 | 정상 동작 | 서버 API에서 교사 권한과 담당 반을 다시 확인함 |
| 학생 비밀번호 초기화 | 정상 동작 | 서버 API에서 교사 권한과 담당 반을 다시 확인함 |

## 1-4. 전체관리자 전용 화면·기능

| 기능 | 상태 | 코드에서 확인한 내용 |
| 반 선택 및 전체 학급 관리 | 정상 동작 | 순수 전체관리자 계정은 학급을 선택해 해당 반을 볼 수 있음 |
| 교사 계정 생성·수정·삭제·비밀번호 초기화 | 정상 동작 | `/api/admin/*`가 관리자 세션을 확인한 뒤 서비스 권한으로 처리함 |
| 입결 원본 업로드 | 정상 동작 | 관리자 전용 업로드 API 및 업로드 화면 연결 확인 |
| 모집정보 원본 업로드 | 정상 동작 | 관리자 전용 업로드 API 및 업로드 화면 연결 확인 |
| 학년 공통 일정 작성·관리 | 정상 동작 | DB 권한 규칙이 전체관리자만 작성·수정·삭제하도록 제한함 |

## 1-5. 미완성 또는 죽은 코드

### 미완성

| 항목 | 근거 |
|---|---|
| 자동 시험 체계 | `package.json`에 일반적인 테스트 실행 명령이 없고, 단위/통합 시험 파일도 확인하지 못했습니다. 기능 코드가 미완성이라는 뜻은 아니지만, 변경 후 자동으로 안전성을 확인하는 장치가 없는 상태입니다. |
| ESLint 경고 1건 | 합격률계산기에서 `loadCards` 의존성 경고가 남아 있습니다. 오류는 아니지만 “정리되지 않은 신호”입니다. |

### 죽은 코드

현재 실행 코드와 실제 DB에서 **확실하게 죽은 코드 또는 죽은 테이블이라고 판정할 항목은 찾지 못했습니다.**

- 과거의 `admission_methods` 테이블과 접수 전 카드의 단일 일정 컬럼은 과거 변경 이력에는 남아 있지만, 실제 DB에서는 삭제되었고 현재 코드도 참조하지 않습니다. 이는 의도적으로 정리된 이력입니다.
- 화면 구성 요소와 주요 훅은 모두 최소 한 곳 이상에서 참조되고 있습니다.
- `TODO`, `FIXME`, 임시 더미값 표시는 실행 코드에서 확인하지 못했습니다.

---

# 2. 데이터 구조 정리 상태

## 2-1. 실제 공개 테이블과 사용처

| 테이블 | 현재 행 수(점검 시점) | 저장하는 내용 | 주 사용 화면/기능 |
|---|---:|---|---|
| `roster` | 148 | 학생 학번·이름·학년·반, 지망 순위 자동 지정 여부 | 로그인 확인, 학생명단, 반 선택, 원서/상담 대상 |
| `profiles` | 111 | 로그인 계정 역할, 교사 역할·담당 반, 테마·글꼴·동의 상태 | 로그인, 역할 판별, 화면 설정, 관리자 모드 |
| `counseling_slots` | 99 | 상담 날짜·시간 또는 자유 라벨, 예약 학생, 메모 | 학생 상담신청, 교사 상담관리 |
| `slot_favorites` | 15 | 교사가 자주 쓰는 상담 시간대 | 교사 상담관리 |
| `wonseo_cards` | 505 | 학생 원서 카드, 모집·입결 메모, 접수 여부·수험번호·일정 | 학생/교사 수시원서 전체 기능 |
| `wonseo_images` | 11 | 원서 카드와 첨부파일 경로의 연결 | 원서 첨부 이미지 |
| `app_settings` | 1 | 전역 설정값(현재 상담 현황 공개 설정) | 교사 상담관리 |
| `calendar_events` | 114 | 개인·반·학년·원서 일정의 캘린더 스냅샷 | 학생/교사 입시일정 |
| `admission_cutoffs` | 57,951 | 과거 입결 원본 | 대입정보 입결 조회, 최근 입결, 합격률계산기 모델 |
| `admission_offerings` | 18,989 | 이번 입시 모집정보 원본 | 원서 카드 자동완성·전형방법·모집인원 자동 채움 |
| `admission_competition_history` | 23,039 | 작년 원서접수 경쟁률 시계열 | 경쟁률 조회, 합격률계산기의 경쟁률 보조 조회 |
| `admission_probability_saves` | 38 | 학생별 합격 가능성 계산 입력·결과 저장 | 합격률계산기 저장 목록 |
| `admission_competition_saves` | 23 | 학생별 경쟁률 조회 저장 | 대입정보 경쟁률 저장 목록 |

별도로 `wonseo-attachments` 저장소가 있으며, 실제 원서 첨부파일은 이곳에 저장되고 `wonseo_images`가 파일 경로만 보관합니다.

## 2-2. 같은 내용을 여러 원본에서 가져오는 부분

### 입시 데이터의 세 갈래 구조

아래 셋은 역할이 다르므로 “중복 저장 자체”가 잘못된 것은 아닙니다.

| 원본 | 담당하는 사실 | 예시 |
|---|---|---|
| 모집정보 | 올해 어떤 전형으로 몇 명을 뽑고, 어떻게 선발하는지 | 전형방법, 수능최저, 모집인원 |
| 과거 입결 | 이전 연도 합격선·경쟁률 | 50%·70% 컷, 과거 모집인원 |
| 작년 경쟁률 아카이브 | 작년 접수 기간 중 시간대별 경쟁률 변화 | 오전/오후별 지원자 수와 경쟁률 |

문제는 세 원본의 대학·학과·전형 이름이 완전히 같지 않다는 점입니다. 현재 코드는 이를 다음 방식으로 각 기능에서 따로 보정합니다.

- 확인된 전형명 별칭 목록
- 전형 종류(교과·종합 등)와 이름 유사도 비교
- `국립` 접두어를 붙이거나 빼서 한 번 더 찾기
- 학과 이름이 다르면 가장 비슷한 학과를 한 번 더 찾기
- 그래도 못 찾으면 사용자가 경쟁률 팝업에서 직접 다시 고르기

따라서 지금도 여러 사례를 처리하지만, **새로운 표기 차이가 들어올 때마다 특정 화면에서만 다시 실패할 수 있는 구조**입니다. 이것이 가장 큰 구조적 정리 대상입니다.

### 원서 일정과 캘린더의 구조

현재는 의도적으로 두 단계입니다.

1. 접수한 원서 카드의 `schedule_events`에 일정 후보를 적습니다.
2. 학생 또는 교사가 “내 원서 일정”에서 원하는 항목만 자기 캘린더에 복사합니다.

캘린더에 복사된 것은 원서 카드와 자동 동기화하지 않는 독립된 기록입니다. 학생이 추가한 원서 일정은 학생만, 교사가 추가한 원서 일정은 그 교사만 보도록 되어 있습니다. 이 구조는 학생·교사 일정이 서로 영향을 주지 않게 하려는 현재 요구와 일치합니다.

## 2-3. 더 이상 쓰이지 않는 데이터

현재 운영 DB 기준으로 확실히 남아 있는 불필요 테이블은 찾지 못했습니다.

- 이전의 `admission_methods`는 삭제되었고 현재 모집정보 테이블로 대체되었습니다.
- 이전의 접수 전 카드 일정 관련 컬럼(`has_exam_date`, `exam_date_at`, `exam_memo` 등)은 삭제되었습니다. 기존 내용은 카드 메모로 옮기도록 데이터 이전이 수행된 흔적이 있습니다.
- 과거 변경 파일은 남아 있지만, 이는 DB 변경 이력을 보존하기 위한 정상적인 기록입니다.

다만 원서 카드 안의 `recent_results`, `schedule_events`, 모집정보의 `raw`, 계산 저장의 `input`처럼 일부 값은 한 칸에 묶어서 저장하는 JSON 형식입니다. 화면 기능에는 편리하지만, 입력 형식 자체를 DB가 세밀하게 막지는 못합니다. 현재 앱이 정상 형식으로 저장한다는 전제에 의존합니다.

---

# 3. 문제 목록

## 높음

### H-1. 입시 원본 간 이름 차이를 코드 여러 곳에서 따로 보정함

**확인한 사실**

- 모집정보, 과거 입결, 경쟁률 아카이브는 각각 별도 테이블입니다.
- 전형명·대학명·학과명 불일치에 대응하는 코드가 `admission-cutoff-lookup`, `admission-offering-lookup`, `admission-competition-lookup`에 나뉘어 있습니다.
- 최근 Git 이력에도 국민대 전형 별칭 추가, 국립부경대 접두어 보정, 경쟁률 전형명 불일치 보정, 수동 선택 기능이 순차적으로 추가된 기록이 있습니다.

**영향**

같은 대학·학과·전형을 원서 카드에서는 찾는데 경쟁률에서는 못 찾거나, 입결은 연결되는데 모집정보는 비는 일이 다시 생길 수 있습니다. 자동으로 “가장 비슷한 것”을 고르는 기능은 편리하지만, 입시 정보에서는 틀린 전형을 연결하는 위험도 있습니다.

**분류**: 기능 간 충돌 / 중복 로직 / 데이터 정합성 위험

---

### H-2. 합격 가능성 계산용 API가 캐시가 비면 과거 입결 전체를 읽음

**확인한 사실**

- `/api/admission-cut-model`은 `admission_cutoffs`에서 수시·교과 데이터를 연도별로 읽고 계산용 데이터베이스를 만듭니다.
- 실제 과거 입결 테이블은 약 57,951행입니다.
- 코드상 15분 메모리 캐시가 있지만, 서버가 새로 시작되면 다시 전체를 읽습니다.
- 코드 주석에도 이전에 조회 시간 초과가 있었고, 이를 커서 방식 조회와 캐시로 개선했다고 명시되어 있습니다.

**영향**

평소에는 동작해도, 서버가 새로 시작된 직후나 여러 서버로 나뉘는 상황에서는 첫 계산이 느리거나 실패할 수 있습니다. 학생들이 한꺼번에 계산기를 열면 더 눈에 띌 수 있습니다.

**분류**: 성능 위험

---

## 중간

### M-1. 학년 공통 일정은 로그인하지 않은 접근에도 보이도록 권한 규칙이 작성됨

**확인한 사실**

- `calendar_events` 읽기 권한은 `calendar_events_can_view` 함수를 사용합니다.
- 이 함수의 `grade` 일정 분기는 조건 없이 `true`를 반환합니다.
- 해당 테이블의 정책 대상 역할은 `public`이며, DB 함수는 익명 사용자도 실행 가능한 상태입니다.
- 반 일정과 개인 일정은 별도의 로그인·반·작성자 검사를 합니다.

**영향**

학년 공통 일정의 제목·날짜·색상은 앱에 로그인하지 않은 외부 사용자도 API로 읽을 수 있는 구조입니다. 현재 데이터 제약상 학년 일정에는 학생 학번이 들어갈 수 없으므로 학생 개인정보가 직접 노출되는 형태는 아니지만, “학생·교사 전체에게만 공개”가 의도였다면 권한 설정이 맞지 않습니다.

**분류**: 권한 처리 확인 필요

---

### M-2. 목록 불러오기 실패가 빈 목록처럼 보일 수 있음

**확인한 사실**

- `useRoster`, `useCounselingSlots`, `useCalendarEvents`, `useFavorites`는 조회 결과의 `error`를 대부분 확인하지 않고 `data ?? []`로 처리합니다.
- 원서 일정 팝업은 후보를 불러오는 Promise에 실패 처리가 없어, 실패하면 로딩 표시가 계속 남을 수 있습니다.

**영향**

네트워크 문제, 권한 정책 오류, 일시적인 DB 오류가 생겨도 사용자에게는 “등록된 학생이 없음”, “일정 없음”처럼 보일 수 있습니다. 특히 담임이 실제 학생·원서가 사라진 것으로 오해할 수 있습니다.

**분류**: 에러 처리 누락

---

### M-3. 교사와 학생 화면에 원서 순서·수정 로직이 중복됨

**확인한 사실**

- `WonseoTab`과 `WonseoManageTab`에 카드 재정렬, 순위 수정, 접수 완료 순서 수정, 자동 순위 설정 등의 유사한 DB 수정 코드가 각각 있습니다.
- 카드 표시 자체는 `WonseoCardView`를 공유하지만, 목록 제어는 두 파일에 나뉘어 있습니다.

**영향**

한쪽에서 순위 규칙을 고치면 다른 쪽을 빠뜨릴 가능성이 있습니다. 실제로 접수 전/접수 후 독립 순서 기능이 추가될 때 두 화면 모두 수정된 이력이 있습니다.

**분류**: 중복 로직 / 유지보수 위험

---

### M-4. 상담 슬롯·캘린더·저장 목록의 외래키 인덱스 일부가 없음

**확인한 사실**

운영 DB 성능 진단은 아래 6개 관계에 빠진 인덱스를 보고했습니다.

- 경쟁률 저장: 작성자, 학생
- 합격 가능성 저장: 작성자, 학생
- 캘린더: 작성자
- 원서 이미지: 원서 카드

**영향**

지금 저장 건수는 적어 체감 문제 가능성이 낮습니다. 하지만 학생·저장·이미지가 늘어날수록 삭제나 관계 확인이 느려질 수 있습니다.

**분류**: 성능 위험

---

### M-5. RLS 권한 식이 큰 데이터 조회마다 반복 계산됨

**확인한 사실**

운영 DB 성능 진단은 `admission_cutoffs`, `admission_offerings`, `admission_competition_history`, `profiles`, `app_settings` 등의 권한 규칙에서 로그인 확인 함수를 행마다 다시 계산할 수 있다고 경고했습니다.

**영향**

특히 입결 57,951행, 모집정보 18,989행, 경쟁률 23,039행처럼 큰 테이블에서 검색이 늘면 느려질 수 있습니다. 지금은 일부 조회가 제한·자동완성·캐시로 완화되어 있으나, 데이터가 커질수록 영향을 받습니다.

**분류**: 성능 위험

---

## 낮음

### L-1. 합격률계산기 ESLint 경고 1건

**확인한 사실**

`AdmissionProbabilityCalculator.tsx`에서 `useEffect`가 사용하는 `loadCards`가 의존성 목록에 빠졌다는 경고가 있습니다.

**영향**

현재 실행을 막지는 않지만, 화면 갱신 규칙을 사람이 추론하기 어렵게 만듭니다.

**분류**: 코드 품질 / 잠재적 상태 갱신 문제

---

### L-2. 일부 목록은 한 번에 전체를 받아옴

**확인한 사실**

- 캘린더 훅은 RLS가 허용한 모든 일정을 한 번에 받아서 화면에서 월별로 나눕니다.
- 관리자용 학생 정보 훅은 현재 반의 명단과 별도로 모든 학생 프로필을 읽어 비밀번호 설정 여부를 판단합니다.

**영향**

현재 일정 114건, 학생 명단 148명 규모에서는 큰 문제가 아닙니다. 다만 수년치 일정을 쌓거나 학교 전체 규모가 커지면 월 범위·반 범위로 나눠 받아야 합니다.

**분류**: 성능 위험(장기)

---

### L-3. 데이터베이스 보안 진단의 일반 경고

**확인한 사실**

DB 진단은 여러 `SECURITY DEFINER` 함수가 `authenticated` 역할에서 실행 가능하다고 경고합니다. 실제로 예약·취소 함수는 내부에서 학생/교사·반·본인 여부를 다시 검사하고, 현재 사용자 정보 함수는 자기 정보만 돌려주는 형태입니다. 따라서 이 점만으로 개인정보 유출이 확인된 것은 아닙니다.

**영향**

새 DB 함수를 추가할 때 같은 형태를 무심코 복사하면 권한 누락이 생길 수 있습니다. M-1의 학년 일정 공개 규칙처럼, 함수별 의도를 문서화하고 권한을 최소화할 필요가 있습니다.

**분류**: 권한 처리 관리 필요

---

# 4. 정리 제안 — 권장 작업 순서

## 1순위. 입시 데이터 이름 대응을 한 곳에서 관리하기

**작업 크기: 큰 작업**  
**의존 관계: 다른 입결·경쟁률 기능 개선보다 먼저 권장**

### 왜 필요한가

지금은 “같은 전형인데 원본마다 이름이 다름”을 여러 화면이 제각각 해결합니다. 문제가 생길 때마다 특정 대학의 별칭을 코드에 추가하면, 당장은 고쳐져도 다른 화면에서 또 누락될 수 있습니다.

### 권장 방향

대학명·학과명·전형명 사이의 “이 이름들은 같은 대상을 뜻한다”는 대응표를 데이터베이스에 별도로 두고, 모집정보·입결·경쟁률 조회가 공통으로 사용하게 만듭니다. 자동으로 확신할 수 없는 경우에는 지금처럼 직접 선택하게 하되, 선택 결과를 관리자 검토용 후보로 남기는 방식이 안전합니다.

### 기대 효과

- 부경대 같은 이름 차이 문제를 한 번에 관리
- “이 화면에서는 나오고 저 화면에서는 안 나오는” 문제 감소
- 매년 새 원본을 올린 뒤 점검할 목록을 만들기 쉬움

## 2순위. 학년 공통 일정의 공개 범위 결정 후 권한 규칙 정리

**작업 크기: 간단**  
**의존 관계: 없음**

### 왜 필요한가

현재 코드는 학년 공통 일정이 로그인 없이도 읽히는 상태입니다. 의도가 정말 공개 일정이면 문서에 명시하면 되고, 학생·교사 로그인 사용자만 봐야 한다면 권한 규칙을 바로잡아야 합니다.

### 결정할 것

- 학교 밖 사람도 봐도 되는 공개 일정인가?
- 아니면 로그인한 학생·교사에게만 보이는 내부 일정인가?

후자라면 해당 일정 읽기 규칙에 로그인 여부 조건을 추가하는 작은 작업으로 해결할 수 있습니다.

## 3순위. 데이터 불러오기 실패를 “빈 목록”과 구분해서 보여 주기

**작업 크기: 보통**  
**의존 관계: 없음**

### 왜 필요한가

지금은 인터넷·권한·DB 오류가 나도 목록이 비어 보일 수 있습니다. 운영 중 가장 어려운 종류의 문제는 “데이터가 없는 것인지, 불러오기에 실패한 것인지”를 구분 못 하는 경우입니다.

### 권장 방향

공통 목록 훅에 `error` 상태를 추가하고, 화면에는 아래처럼 구분해 표시합니다.

- 실제 데이터 없음: “등록된 일정이 없습니다.”
- 불러오기 실패: “불러오지 못했습니다. 다시 시도해 주세요.” + 다시 불러오기 버튼

원서 일정 팝업도 실패 시 로딩 상태가 끝나고 안내가 보이도록 정리합니다.

## 4순위. 합격 가능성 계산 자료를 미리 만들어 두는 방식 검토

**작업 크기: 큰 작업**  
**의존 관계: 1순위와는 독립적**

### 왜 필요한가

현재는 서버가 처음 계산기를 열 때 큰 입결 원본을 읽어 계산용 묶음을 만듭니다. 학생이 몰리는 시간에는 첫 접속이 느릴 수 있습니다.

### 권장 방향

입결 원본을 업로드할 때 계산용 집계 자료도 함께 미리 만들거나, 별도 테이블에 저장해 두고 계산기에서는 그것만 읽도록 합니다. 이렇게 하면 실제 계산 화면은 필요한 작은 자료만 받아 훨씬 안정적입니다.

### 주의

입결 업로드 흐름과 연결되므로, “어느 원본 연도를 계산에 쓸지”를 먼저 정해야 합니다.

## 5순위. 원서 목록 조작 로직을 공통화

**작업 크기: 보통**  
**의존 관계: 없음**

### 왜 필요한가

학생 화면과 교사 화면이 같은 원서 데이터를 다루지만, 정렬·순위·접수 완료 순서 저장 코드를 각각 갖고 있습니다. 기능을 하나 더 추가할 때 두 군데를 함께 고쳐야 합니다.

### 권장 방향

원서 카드의 순서 변경, 순위 변경, 접수 완료 정보 저장을 공통 함수 또는 공통 훅으로 옮기고, 화면은 “누가 보고 있는가”만 넘기도록 정리합니다.

## 6순위. DB 성능 진단 항목 정리

**작업 크기: 간단~보통**  
**의존 관계: 4순위와 병행 가능**

### 왜 필요한가

현재 데이터 크기에서는 급하지 않지만, 저장 목록·첨부파일·캘린더가 늘면 DB가 불필요하게 많이 찾을 수 있습니다.

### 권장 순서

1. 저장 목록·원서 이미지·캘린더의 빠진 외래키 인덱스 추가
2. 큰 입시 원본 테이블의 권한 확인식을 행마다 반복하지 않도록 개선
3. 월별 캘린더, 반별 학생 프로필처럼 필요한 범위만 읽도록 조회 범위 정리
4. 실제 사용하지 않는 인덱스는 충분한 운영 기간을 본 뒤에만 삭제 여부 판단

## 7순위. 정적 검사 경고 제거와 최소 자동 시험 추가

**작업 크기: 간단~보통**  
**의존 관계: 없음**

### 왜 필요한가

기능이 빠르게 늘어나는 앱에서는 작은 수정이 기존 기능을 깨기 쉽습니다. 현재는 사람이 직접 확인하는 비중이 큽니다.

### 최소 시작안

- ESLint 경고 1건 제거
- 권한이 중요한 예약·취소, 원서 일정 분리, 원서 순서 분리, 전형명 대응 규칙만이라도 자동 확인
- 업로드 원본이 바뀐 뒤 “연결되지 않는 대학·학과·전형” 목록을 자동으로 뽑는 점검 기능 추가

---

# 부록: 앞으로 작업 계획을 세울 때의 추천 묶음

| 단계 | 목표 | 포함 작업 |
|---|---|---|
| A. 안전성 | 권한과 오류 안내를 먼저 안정화 | 2순위, 3순위, ESLint 경고 정리 |
| B. 데이터 신뢰성 | 입시 정보가 화면마다 다르게 보이는 문제 줄이기 | 1순위, 업로드 후 불일치 점검 |
| C. 속도 | 학생이 몰려도 계산·검색이 안정적으로 열리게 하기 | 4순위, 6순위 |
| D. 유지보수 | 다음 기능 추가 비용 줄이기 | 5순위, 자동 시험 추가 |

가장 먼저 시작할 실무 순서는 **A → B → C → D**를 권장합니다. 특히 권한 공개 범위와 오류 안내는 기능을 더 늘리기 전에 정해 두는 편이 운영 부담을 줄입니다.
